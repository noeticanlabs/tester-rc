// @cohbit/code-atlas — L12 Governance / Versioning Layer
export type CanonicalStatus = 'experimental' | 'draft' | 'canonical_candidate' | 'canonical' | 'deprecated' | 'superseded';
export interface AtlasVersionRecord { versionId: string; entryId: string; changeType: 'created' | 'modified' | 'deprecated' | 'superseded'; claimStatus: 'draft_engineering_claim' | 'receipted_claim' | 'canonical_claim'; canonicalStatus: CanonicalStatus; evidenceRequired: string[]; changedAt: string; }
export function createVersionRecord(params: { entryId: string; changeType: 'created' | 'modified' | 'deprecated' | 'superseded'; claimStatus?: 'draft_engineering_claim' | 'receipted_claim' | 'canonical_claim'; canonicalStatus?: CanonicalStatus; evidenceRequired?: string[] }): AtlasVersionRecord { return { versionId: `VER_${Date.now().toString(36)}`, entryId: params.entryId, changeType: params.changeType, claimStatus: params.claimStatus ?? 'draft_engineering_claim', canonicalStatus: params.canonicalStatus ?? 'experimental', evidenceRequired: params.evidenceRequired ?? [], changedAt: new Date().toISOString() }; }