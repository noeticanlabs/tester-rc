// @cohbit/math-atlas — M10 Simulation / Computation Layer
export interface SimulationRecord { simulationRecordId: string; mathArtifactId: string; simulationType: string; modelFamily: string; testedCases: number; passedCases: number; observedPattern: string; claimSupported: string; claimNotSupported: string[]; status: 'evidence_recorded'; createdAt: string; }
export function createSimulationRecord(params: { mathArtifactId: string; simulationType: string; modelFamily: string; testedCases: number; passedCases: number; observedPattern: string; claimSupported: string; claimNotSupported?: string[] }): SimulationRecord { return { simulationRecordId: `MSIM_${Date.now().toString(36)}`, ...params, claimNotSupported: params.claimNotSupported ?? ['universal theorem', 'formal proof'], status: 'evidence_recorded', createdAt: new Date().toISOString() }; }