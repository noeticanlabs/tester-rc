// @cohbit/math-atlas — M13 Receipt Layer
export type MathReceiptStatus = 'accepted' | 'accepted_with_limitations' | 'rejected' | 'repair_required' | 'clarification_required' | 'proof_pending' | 'simulation_supported' | 'analogy_only' | 'stale' | 'superseded';
export interface MathReceiptRecord { mathReceiptId: string; sourceArtifactId: string; claimType: string; sourceModel: string; targetModel: string; preservedInvariants: string[]; evidenceLevel: string; knownLimitations: string[]; status: MathReceiptStatus; reusePermission: string; createdAt: string; }
export function createMathReceipt(params: { sourceArtifactId: string; sourceModel: string; targetModel: string; preservedInvariants: string[]; evidenceLevel: string; status: MathReceiptStatus; knownLimitations?: string[] }): MathReceiptRecord { return { mathReceiptId: `MRCPT_${Date.now().toString(36)}`, claimType: 'mathematical_invariant_mapping_claim', ...params, knownLimitations: params.knownLimitations ?? [], reusePermission: 'allowed_with_model_scope_warning', createdAt: new Date().toISOString() }; }