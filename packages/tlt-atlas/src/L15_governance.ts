// @cohbit/tlt-atlas — L15 Governance / Versioning Layer
export interface LangGovernanceRecord { recordId: string; entryId: string; changeType: 'created' | 'modified' | 'deprecated' | 'superseded'; claimStatus: 'draft' | 'reviewed' | 'canonical'; canonicalStatus: 'experimental' | 'canonical'; changedAt: string; }
export const FORBIDDEN_LANG_COLLAPSES: string[] = ['word match ≠ meaning match', 'translation ≠ cultural fit', 'literal translation ≠ usable translation', 'fluent translation ≠ accurate translation', 'formal tone ≠ better meaning', 'technical term ≠ same term in every language', 'backtranslation ≠ full proof'];
export function createLangGovernanceRecord(params: { entryId: string; changeType: 'created' | 'modified' | 'deprecated' | 'superseded' }): LangGovernanceRecord { return { recordId: `LGOV_${Date.now().toString(36)}`, entryId: params.entryId, changeType: params.changeType, claimStatus: 'draft', canonicalStatus: 'experimental', changedAt: new Date().toISOString() }; }