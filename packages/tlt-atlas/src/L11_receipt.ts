// @cohbit/tlt-atlas — L11 Language Receipt Layer
export type LangReceiptStatus = 'accepted' | 'accepted_with_limitations' | 'rejected' | 'repair_required' | 'clarification_required' | 'stale' | 'superseded';
export interface LanguageReceiptRecord { languageReceiptId: string; sourceArtifactId: string; sourceLanguage: string; targetLanguage: string; meaningInvariantId: string; projectionId: string; evidenceLevel: string; status: LangReceiptStatus; limitations: string[]; reusePermission: string; createdAt: string; }
export function createLanguageReceipt(params: { sourceArtifactId: string; sourceLanguage: string; targetLanguage: string; meaningInvariantId: string; projectionId: string; evidenceLevel: string; status: LangReceiptStatus; limitations?: string[]; reusePermission?: string }): LanguageReceiptRecord { return { languageReceiptId: `LRCPT_${Date.now().toString(36)}`, ...params, limitations: params.limitations ?? [], reusePermission: params.reusePermission ?? 'allowed_with_context_warning', createdAt: new Date().toISOString() }; }